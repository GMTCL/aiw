# ⚡ Quick Start Guide

## 🎯 สถานะปัจจุบัน

✅ โค้ดพร้อมใช้งาน 100%  
⚠️ ต้องติดตั้ง Python ก่อน  
⚠️ ต้องมี API Token จาก Replicate  

---

## 📦 ติดตั้ง Python (ถ้ายังไม่มี)

⚠️ **สำคัญ: ใช้ Python 3.11 หรือ 3.12 เท่านั้น!**
❌ **อย่าใช้ Python 3.14** - replicate package ยังไม่รองรับ

### วิธีเร็วที่สุด - Microsoft Store:
1. กด `Windows + S`
2. พิมพ์ "Microsoft Store"
3. ค้นหา **"Python 3.12"** (ระบุเวอร์ชันชัดเจน)
4. กด "Get" หรือ "ติดตั้ง"

### หรือดาวน์โหลดจาก:
https://www.python.org/downloads/
(เลือก Python 3.12.x ไม่ใช่ 3.14)

**สำคัญ:** เลือก "Add Python to PATH" ตอนติดตั้ง!

---

## 🚀 เริ่มใช้งาน (4 ขั้นตอน)

### 1️⃣ ติดตั้ง Dependencies

เปิด Command Prompt หรือ PowerShell ในโฟลเดอร์โปรเจกต์:

```bash
pip install -r requirements.txt
```

หรือ:
```bash
pip install flask flask-cors replicate python-dotenv requests
```

### 2️⃣ สมัคร Replicate API (ฟรี)

1. ไปที่: https://replicate.com
2. สมัครสมาชิก (ใช้ Google/GitHub)
3. ไปที่: Account Settings → API Tokens
4. คัดลอก token

### 3️⃣ สร้างไฟล์ .env

สร้างไฟล์ชื่อ `.env` ในโฟลเดอร์โปรเจกต์:

```
REPLICATE_API_TOKEN=r8_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

(แทนที่ด้วย token จริงของคุณ)

### 4️⃣ รันโปรแกรม

```bash
python app.py
```

เปิดเบราว์เซอร์: http://localhost:5000

---

## 🧪 ทดสอบระบบ

ก่อนรันโปรแกรม ทดสอบว่าทุกอย่างพร้อมหรือยัง:

```bash
python test_setup.py
```

สคริปต์นี้จะตรวจสอบ:
- ✅ Python version
- ✅ Packages ติดตั้งครบหรือยัง
- ✅ ไฟล์ .env มี API token หรือยัง
- ✅ โครงสร้างโปรเจกต์ถูกต้องหรือไม่
- ✅ Port 5000 ว่างหรือไม่

---

## 📁 โครงสร้างโปรเจกต์

```
vdoai/
├── app.py                  # Backend server
├── static/
│   └── index.html         # Frontend UI
├── videos/                # วิดีโอที่สร้าง (สร้างอัตโนมัติ)
├── requirements.txt       # Dependencies
├── .env                   # API token (สร้างเอง)
├── test_setup.py          # สคริปต์ทดสอบ
├── README.md              # เอกสารฉบับเต็ม
├── INSTALL_GUIDE.md       # คู่มือติดตั้ง Python
└── QUICK_START.md         # ไฟล์นี้
```

---

## 💡 ตัวอย่างการใช้งาน

1. เปิด http://localhost:5000
2. พิมพ์คำอธิบาย (ภาษาอังกฤษ):
   - "A cat playing with a ball"
   - "Fireworks in the night sky"
   - "Ocean waves at sunset"
3. เลือกระยะเวลา (2-8 วินาที)
4. กด "สร้างวิดีโอ"
5. รอ 1-3 นาที
6. ดูและดาวน์โหลดวิดีโอ

---

## 🆘 แก้ปัญหาเร็ว

### Error "pydantic.v1.errors.ConfigError" หรือ "Python 3.14 not compatible"
→ **ใช้ Python 3.14 ซึ่งยังไม่รองรับ!**
→ ถอน Python 3.14 แล้วติดตั้ง Python 3.12 ใหม่
→ ดูรายละเอียดใน `INSTALL_GUIDE.md`

### "python is not recognized"
→ ติดตั้ง Python แล้ว restart Command Prompt

### "pip is not recognized"
→ ใช้: `python -m pip install -r requirements.txt`

### "ไม่พบ API token"
→ ตรวจสอบไฟล์ .env มี token ถูกต้อง

### "Port 5000 is already in use"
→ ปิดโปรแกรมที่ใช้ port 5000 หรือเปลี่ยน port ใน app.py

### วิดีโอสร้างช้า
→ ปกติ! AI ต้องใช้เวลา 1-3 นาที

---

## ✅ Checklist

- [ ] ติดตั้ง Python แล้ว (`python --version`)
- [ ] ติดตั้ง packages แล้ว (`pip list`)
- [ ] สมัคร Replicate แล้ว
- [ ] สร้างไฟล์ .env และใส่ token แล้ว
- [ ] รัน `python test_setup.py` ผ่านทุกข้อ
- [ ] รัน `python app.py` สำเร็จ
- [ ] เปิด http://localhost:5000 ได้

---

## 🎉 พร้อมแล้ว!

ถ้าทุกอย่างเรียบร้อย ก็เริ่มสร้างวิดีโอได้เลย! 🎬
