# 🔧 แก้ปัญหา Python 3.14

## ⚠️ ปัญหา

คุณติดตั้ง Python 3.14 แล้ว แต่ replicate package ยังไม่รองรับ Python 3.14
จะเกิด error: `pydantic.v1.errors.ConfigError: unable to infer type for attribute "previous"`

## ✅ วิธีแก้ไข (2 ทางเลือก)

### วิธีที่ 1: ถอน Python 3.14 แล้วติดตั้ง 3.12 (แนะนำ)

#### ขั้นตอนที่ 1: ถอน Python 3.14
1. กด `Windows + I` เปิด Settings
2. ไปที่ **Apps** → **Installed apps**
3. ค้นหา "Python 3.14"
4. คลิก **...** (three dots) → **Uninstall**
5. ยืนยันการถอน

#### ขั้นตอนที่ 2: ติดตั้ง Python 3.12

**จาก Microsoft Store (แนะนำ):**
1. เปิด Microsoft Store
2. ค้นหา **"Python 3.12"** (ระบุเวอร์ชันชัดเจน)
3. กด **Get** หรือ **Install**
4. รอให้ติดตั้งเสร็จ

**หรือจาก python.org:**
1. ไปที่: https://www.python.org/downloads/
2. เลือก **Python 3.12.x** (ไม่ใช่ 3.14)
3. ดาวน์โหลด Windows installer (64-bit)
4. รันไฟล์ติดตั้ง
5. ✅ **สำคัญ:** เลือก "Add Python to PATH"
6. กด Install Now

#### ขั้นตอนที่ 3: ตรวจสอบ
เปิด Command Prompt ใหม่:
```bash
python --version
```
ควรแสดง: `Python 3.12.x`

#### ขั้นตอนที่ 4: ติดตั้ง packages ใหม่
```bash
python -m pip install -r requirements.txt
```

#### ขั้นตอนที่ 5: ทดสอบ
```bash
python test_setup.py
```

---

### วิธีที่ 2: ใช้ Virtual Environment กับ Python 3.12 (สำหรับผู้ใช้ขั้นสูง)

ถ้าต้องการเก็บ Python 3.14 ไว้ด้วย สามารถติดตั้ง Python 3.12 แยกและใช้ virtual environment:

1. ติดตั้ง Python 3.12 (ไม่ต้องถอน 3.14)
2. ใช้ py launcher เลือก version:
```bash
py -3.12 -m venv venv
venv\Scripts\activate
python -m pip install -r requirements.txt
```

---

## 🎯 ขั้นตอนต่อไปหลังแก้ไข

1. ✅ ติดตั้ง Python 3.12 แล้ว
2. ✅ ติดตั้ง packages แล้ว
3. 📝 สมัคร Replicate API: https://replicate.com
4. 🔑 สร้างไฟล์ `.env` และใส่ API token
5. 🚀 รันโปรแกรม: `python app.py`
6. 🌐 เปิดเบราว์เซอร์: http://localhost:5000

---

## 💡 ทำไมต้องใช้ Python 3.12?

- Python 3.14 เพิ่งออกมา (พฤศจิกายน 2025)
- หลาย package ยังไม่อัปเดตรองรับ
- replicate ใช้ pydantic v1 ซึ่งไม่รองรับ Python 3.14
- Python 3.12 เสถียรและรองรับทุก package ในโปรเจกต์นี้

---

## 🆘 ต้องการความช่วยเหลือ?

ถ้าติดปัญหาตอนถอนหรือติดตั้ง Python ใหม่:
1. ตรวจสอบว่าปิดโปรแกรมที่ใช้ Python ทั้งหมดแล้ว
2. Restart เครื่องหลังถอน Python 3.14
3. ติดตั้ง Python 3.12 ใหม่
4. เปิด Command Prompt ใหม่ทุกครั้ง
