# 📦 คู่มือการติดตั้ง Python และ Dependencies

## ⚠️ สำคัญมาก! เรื่อง Python Version

**ใช้ Python 3.11 หรือ 3.12 เท่านั้น**

❌ **อย่าใช้ Python 3.14** - replicate package ยังไม่รองรับ (จะเกิด pydantic error)
✅ **แนะนำ Python 3.12** - เสถียรและรองรับทุก package

### วิธีตรวจสอบ Python version ที่ติดตั้งอยู่

```bash
python --version
```

ถ้าเป็น Python 3.14 ต้องถอนแล้วติดตั้ง 3.12 ใหม่

---

## 📥 วิธีติดตั้ง Python 3.12

### วิธีที่ 1: ติดตั้งจาก Microsoft Store (แนะนำ - ง่ายที่สุด)

1. กด Windows + S แล้วพิมพ์ "Microsoft Store"
2. ค้นหา **"Python 3.12"** (ระบุเวอร์ชันชัดเจน)
3. กดปุ่ม "Get" หรือ "ติดตั้ง"
4. รอให้ติดตั้งเสร็จ

### วิธีที่ 2: ดาวน์โหลดจากเว็บไซต์

1. ไปที่ https://www.python.org/downloads/
2. เลือก **Python 3.12.x** (ไม่ใช่ 3.14)
3. ดาวน์โหลด Windows installer (64-bit)
4. รันไฟล์ติดตั้ง
5. **สำคัญ:** ✅ เลือก "Add Python to PATH" ก่อนกด Install

---

## 🚀 หลังติดตั้ง Python แล้ว

### 1. ตรวจสอบว่าติดตั้งสำเร็จ

เปิด Command Prompt หรือ PowerShell ใหม่ แล้วพิมพ์:

```bash
python --version
```

ควรแสดงเวอร์ชัน เช่น `Python 3.12.0`

### 2. ติดตั้ง Dependencies

```bash
pip install flask flask-cors replicate python-dotenv requests
```

หรือใช้คำสั่งเดียว:

```bash
pip install -r requirements.txt
```

### 3. สร้างไฟล์ .env

สร้างไฟล์ชื่อ `.env` ในโฟลเดอร์โปรเจกต์ แล้วใส่:

```
REPLICATE_API_TOKEN=ใส่_token_ของคุณ_ที่นี่
```

### 4. รันโปรแกรม

```bash
python app.py
```

### 5. เปิดเบราว์เซอร์

ไปที่: http://localhost:5000

---

## 🆘 แก้ปัญหา

### ปัญหา: Error "pydantic.v1.errors.ConfigError" หรือ "Python 3.14 not compatible"

**สาเหตุ:** ใช้ Python 3.14 ซึ่ง replicate package ยังไม่รองรับ

**วิธีแก้:**
1. ถอน Python 3.14:
   - Windows Settings → Apps → Python 3.14 → Uninstall
2. ติดตั้ง Python 3.12 ใหม่ (ดูวิธีด้านบน)
3. ติดตั้ง packages ใหม่:
   ```bash
   python -m pip install -r requirements.txt
   ```

### ปัญหา: "pip is not recognized"

ลองใช้:
```bash
python -m pip install -r requirements.txt
```

### ปัญหา: "python is not recognized" (หลังติดตั้งแล้ว)

1. ปิด Command Prompt/PowerShell
2. เปิดใหม่อีกครั้ง
3. ลองอีกครั้ง

หรือ restart เครื่อง

### ปัญหา: Permission denied

รัน Command Prompt/PowerShell แบบ "Run as Administrator"

---

## ✅ Checklist

- [ ] ติดตั้ง Python แล้ว
- [ ] ติดตั้ง pip packages แล้ว
- [ ] สมัคร Replicate API แล้ว (https://replicate.com)
- [ ] สร้างไฟล์ .env และใส่ API token แล้ว
- [ ] รัน `python app.py` สำเร็จ
- [ ] เปิด http://localhost:5000 ได้

---

## 📞 ติดปัญหา?

ตรวจสอบว่า:
1. Python ติดตั้งถูกต้อง (ลอง `python --version`)
2. Packages ติดตั้งครบ (ลอง `pip list`)
3. ไฟล์ .env มี API token
4. Port 5000 ไม่ถูกใช้งานโดยโปรแกรมอื่น
